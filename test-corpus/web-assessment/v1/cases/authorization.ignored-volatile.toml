schema = "security-assessment-fixture/v1"
id = "authorization.ignored-volatile"
revision = 1
category = "authorization"
purpose = "Ignore one declared volatile field while requiring stable equivalence for the selected resource."
provenance = "current-authored"
support = "current"
tags = ["authorization", "ignored-path", "positive", "replay"]

[request]
method = "get"
origin = "https://api.example.test"
path = "/api/accounts/fixture-volatile"
role = "bootstrap"

[response]
status = 200
media_type = "application/json"
role = "bootstrap"
completion = "complete"
truncated = false
body_file = "bodies/authorization-resource-a.json"

[authorization]
resource = "/api/accounts/fixture-volatile"
resource_handle = "fixture-volatile-account"
expectation = "primary-only"
method = "GET"
comparison = { selected_paths = ["/data/account"], ignored_paths = ["/data/account/updated_at"], unordered_array_paths = [], max_diff_paths = 16 }
primary_candidate = { status = 200, media_type = "application/json", completion = "complete", truncated = false, state = "complete-json", body_file = "bodies/authorization-resource-a.json" }
peer_candidate = { status = 200, media_type = "application/json", completion = "complete", truncated = false, state = "complete-json", body_file = "bodies/authorization-resource-a-volatile.json" }
primary_replay = { status = 200, media_type = "application/json", completion = "complete", truncated = false, state = "complete-json", body_file = "bodies/authorization-resource-a-volatile.json" }
peer_replay = { status = 200, media_type = "application/json", completion = "complete", truncated = false, state = "complete-json", body_file = "bodies/authorization-resource-a.json" }

[expected]
authorization_outcome = "stable-cross-principal-equivalence"
assessment_capability = "authorization.resource-cross-principal-equivalence"
maximum_disposition = "needs-review"
maximum_authority = "knowledge-only"
