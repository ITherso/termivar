schema = "security-assessment-fixture/v1"
id = "ssti.unsupported-media"
revision = 1
category = "ssti"
purpose = "Keep unsupported response media outside SSTI semantic evaluation."
provenance = "current-authored"
support = "current"
tags = ["ssti", "unsupported", "media"]

[request]
method = "get"
origin = "https://assets.example.test"
path = "/review/ssti/unsupported"
role = "bootstrap"

[[request.query]]
name = "view"
value = "fixture-unsupported-case"

[response]
status = 200
control_status = 200
control_body_file = "bodies/ssti-control.html"
replay_status = 200
replay_body_file = "bodies/ssti-unsupported.txt"
media_type = "application/octet-stream"
role = "bootstrap"
completion = "complete"
truncated = false
body_file = "bodies/ssti-unsupported.txt"

[expected]
ssti_relation = "not-matched"
incompleteness = "unsupported-by-runtime"
