schema = "security-assessment-fixture/v1"
id = "graphql.get-query-metadata-only"
revision = 1
category = "api-graphql"
purpose = "Retain harmless GET query taxonomy as metadata while V1 execution remains POST-only."
provenance = "current-authored"
support = "metadata-only"
tags = ["get", "graphql", "metadata"]

[request]
method = "get"
origin = "https://api.example.test"
path = "/graphql"
role = "bootstrap"

[[request.query]]
name = "query"
value = "query CorpusGet { fixtureGet: __typename }"

[response]
status = 200
media_type = "application/graphql-response+json"
role = "bootstrap"
completion = "complete"
truncated = false
body_file = "bodies/graphql-get-control.json"

[expected]
graphql_evidence = "get-query-metadata-only"
maximum_authority = "knowledge-only"
incompleteness = "future-metadata-only"
