schema = "security-assessment-fixture/v1"
id = "graphql.partial-data-errors"
revision = 1
category = "api-graphql"
purpose = "Keep partial data with structured errors conservative until an exact response contract exists."
provenance = "current-authored"
support = "metadata-only"
tags = ["errors", "graphql", "partial"]

[request]
method = "post"
origin = "https://api.example.test"
path = "/graphql"
role = "bootstrap"
body_media_type = "application/json"
inline_body = '''{"query":"query CorpusPartial { fixturePartial: __typename __schema { queryType { name } } }"}'''

[response]
status = 200
media_type = "application/graphql-response+json"
role = "bootstrap"
completion = "complete"
truncated = false
body_file = "bodies/graphql-partial.json"

[expected]
graphql_evidence = "partial-data-with-errors"
maximum_authority = "knowledge-only"
incompleteness = "future-metadata-only"
