schema = "security-assessment-fixture/v1"
id = "graphql.partial-data-errors"
revision = 1
category = "api-graphql"
purpose = "Exercise the fail-closed partial-data-with-errors response contract."
provenance = "current-authored"
support = "current"
tags = ["errors", "graphql", "partial"]

[request]
method = "post"
origin = "https://api.example.test"
path = "/graphql"
role = "bootstrap"
body_media_type = "application/json"
inline_body = '''{"query":"query VenomGraphqlCandidateV1 { venomCandidateV1: __typename __schema { queryType { name } mutationType { name } subscriptionType { name } } }"}'''

[response]
status = 200
media_type = "application/graphql-response+json"
role = "bootstrap"
completion = "complete"
truncated = false
body_file = "bodies/graphql-partial.json"

[expected]
graphql_evidence = "partial-data-with-errors"
maximum_authority = "knowledge-only"
